{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "wrangler pages deploy dist --commit-dirty=true",
    "cf:login": "wrangler login",
    "cf:build": "npm run build && npm run deploy"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "hls.js": "^1.6.8",
    "nuxt": "^4.0.1",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@nuxthub/core": "^0.9.0",
    "wrangler": "^4.38.0"
  }
}
